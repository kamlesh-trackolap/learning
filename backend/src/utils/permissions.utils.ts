export interface permissionTypes {
    key:number;
    value:string;
}
const permissions: permissionTypes[]  = [
    {
        key:1,
        value:"formBuilderDelete"
    }
]
export default permissions;